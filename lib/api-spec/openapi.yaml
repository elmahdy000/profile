openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: bookings
    description: Booking operations
  - name: podcasts
    description: Podcast operations
  - name: courses
    description: Course operations
  - name: settings
    description: Site settings operations
paths:
  /settings:
    get:
      operationId: getSettings
      tags: [settings]
      summary: Get all settings
      description: Returns all site settings as a key-value map
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  $ref: "#/components/schemas/SiteSetting"
    post:
      operationId: setSetting
      tags: [settings]
      summary: Update setting
      description: Update or create a site setting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertSiteSetting"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettingModel"
  /settings/batch:
    put:
      operationId: updateSettingsBatch
      tags: [settings]
      summary: Update settings batch
      description: Update multiple settings at once
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                settings:
                  type: array
                  items:
                    $ref: "#/components/schemas/InsertSiteSetting"
              required:
                - settings
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SiteSettingModel"
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /bookings:
    get:
      operationId: listBookings
      tags: [bookings]
      summary: List bookings
      description: Returns all bookings
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"
    post:
      operationId: createBooking
      tags: [bookings]
      summary: Create booking
      description: Submits a new booking request
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertBooking"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
  /bookings/{id}:
    put:
      operationId: updateBookingStatus
      tags: [bookings]
      summary: Update booking status
      description: Updates the status of an existing booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
              required:
                - status
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
    delete:
      operationId: deleteBooking
      tags: [bookings]
      summary: Delete booking
      description: Deletes a booking from the database
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success
  /podcasts:
    get:
      operationId: listPodcasts
      tags: [podcasts]
      summary: List podcasts
      description: Returns all podcast episodes
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Podcast"
    post:
      operationId: createPodcast
      tags: [podcasts]
      summary: Create podcast
      description: Creates a new podcast episode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertPodcast"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Podcast"
  /podcasts/{id}:
    put:
      operationId: updatePodcast
      tags: [podcasts]
      summary: Update podcast
      description: Updates an existing podcast episode
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertPodcast"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Podcast"
    delete:
      operationId: deletePodcast
      tags: [podcasts]
      summary: Delete podcast
      description: Deletes a podcast episode
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success
  /courses:
    get:
      operationId: listCourses
      tags: [courses]
      summary: List courses
      description: Returns all courses
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Course"
    post:
      operationId: createCourse
      tags: [courses]
      summary: Create course
      description: Creates a new course
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertCourse"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{id}:
    put:
      operationId: updateCourse
      tags: [courses]
      summary: Update course
      description: Updates an existing course
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertCourse"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
    delete:
      operationId: deleteCourse
      tags: [courses]
      summary: Delete course
      description: Deletes a course
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success
  /videos:
    get:
      operationId: listVideos
      tags: [videos]
      summary: List videos and playlists
      description: Returns all videos and playlists
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Video"
    post:
      operationId: createVideo
      tags: [videos]
      summary: Create video or playlist
      description: Creates a new video or playlist entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertVideo"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
  /videos/{id}:
    put:
      operationId: updateVideo
      tags: [videos]
      summary: Update video or playlist
      description: Updates an existing video or playlist entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertVideo"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
    delete:
      operationId: deleteVideo
      tags: [videos]
      summary: Delete video or playlist
      description: Deletes a video or playlist entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success
  /curriculums:
    get:
      operationId: listCurriculums
      tags: [curriculums]
      summary: List curriculums
      description: Returns all curriculum lessons
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Curriculum"
    post:
      operationId: createCurriculum
      tags: [curriculums]
      summary: Create curriculum lesson
      description: Creates a new curriculum lesson with images
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertCurriculum"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curriculum"
  /curriculums/{id}:
    put:
      operationId: updateCurriculum
      tags: [curriculums]
      summary: Update curriculum lesson
      description: Updates an existing curriculum lesson
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InsertCurriculum"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curriculum"
    delete:
      operationId: deleteCurriculum
      tags: [curriculums]
      summary: Delete curriculum lesson
      description: Deletes a curriculum lesson
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    InsertBooking:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        message:
          type: string
      required:
        - name
        - phone
        - message
    Booking:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        phone:
          type: string
        message:
          type: string
        status:
          type: string
        createdAt:
          type: string
      required:
        - id
        - name
        - phone
        - message
        - status
        - createdAt
    InsertPodcast:
      type: object
      properties:
        title:
          type: string
        desc:
          type: string
        duration:
          type: string
        youtubeUrl:
          type: string
          nullable: true
        audioUrl:
          type: string
          nullable: true
      required:
        - title
        - desc
        - duration
    Podcast:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        desc:
          type: string
        duration:
          type: string
        youtubeUrl:
          type: string
          nullable: true
        audioUrl:
          type: string
          nullable: true
        createdAt:
          type: string
      required:
        - id
        - title
        - desc
        - duration
        - createdAt
    InsertCourse:
      type: object
      properties:
        title:
          type: string
        age:
          type: string
        duration:
          type: string
        sessions:
          type: string
        level:
          type: string
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        img:
          type: string
      required:
        - title
        - age
        - duration
        - sessions
        - level
        - category
        - tags
        - img
    Course:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        age:
          type: string
        duration:
          type: string
        sessions:
          type: string
        level:
          type: string
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        img:
          type: string
        lessonsCount:
          type: integer
        videosCount:
          type: integer
      required:
        - id
        - title
        - age
        - duration
        - sessions
        - level
        - category
        - tags
        - img
        - lessonsCount
        - videosCount
    InsertSiteSetting:
      type: object
      properties:
        key:
          type: string
        value:
          type: string
          nullable: true
        type:
          type: string
      required:
        - key
    SiteSetting:
      type: object
      properties:
        value:
          type: string
          nullable: true
        type:
          type: string
    SiteSettingModel:
      type: object
      properties:
        id:
          type: integer
        key:
          type: string
        value:
          type: string
          nullable: true
        type:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - key
        - updatedAt
    InsertCurriculum:
      type: object
      properties:
        subject:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        images:
          type: array
          items:
            type: string
        order:
          type: integer
      required:
        - subject
        - title
        - images
    Curriculum:
      type: object
      properties:
        id:
          type: integer
        subject:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        images:
          type: array
          items:
            type: string
        order:
          type: integer
        createdAt:
          type: string
      required:
        - id
        - subject
        - title
        - images
        - order
        - createdAt
    InsertVideo:
      type: object
      properties:
        courseId:
          type: integer
          nullable: true
        category:
          type: string
        stage:
          type: string
          nullable: true
        stages:
          type: array
          items:
            type: string
        subject:
          type: string
          nullable: true
        learningMode:
          type: string
          enum: [online, offline, both]
          default: online
        tags:
          type: array
          items:
            type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        youtubeUrl:
          type: string
        type:
          type: string
        order:
          type: integer
        isProtected:
          type: boolean
          default: false
        isPublished:
          type: boolean
          default: true
        accessKey:
          type: string
          nullable: true
        durationText:
          type: string
          nullable: true
        lessonsCount:
          type: integer
          nullable: true
        level:
          type: string
          nullable: true
        pdfFileId:
          type: integer
          nullable: true
        attachmentFileIds:
          type: array
          items:
            type: integer
        quizId:
          type: integer
          nullable: true
      required:
        - category
        - title
        - youtubeUrl
        - type
    Video:
      type: object
      properties:
        id:
          type: integer
        courseId:
          type: integer
          nullable: true
        category:
          type: string
        stage:
          type: string
          nullable: true
        stages:
          type: array
          items:
            type: string
        subject:
          type: string
          nullable: true
        learningMode:
          type: string
          enum: [online, offline, both]
        tags:
          type: array
          items:
            type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        youtubeUrl:
          type: string
        type:
          type: string
        order:
          type: integer
        isProtected:
          type: boolean
        isPublished:
          type: boolean
        accessKey:
          type: string
          nullable: true
        durationText:
          type: string
          nullable: true
        lessonsCount:
          type: integer
          nullable: true
        level:
          type: string
          nullable: true
        pdfFileId:
          type: integer
          nullable: true
        attachments:
          type: array
          items:
            type: object
            additionalProperties: true
        quizId:
          type: integer
          nullable: true
        createdAt:
          type: string
      required:
        - id
        - category
        - title
        - youtubeUrl
        - type
        - order
        - isProtected
        - isPublished
        - createdAt
