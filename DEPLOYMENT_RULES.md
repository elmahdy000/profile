# 🛡️ قواعد ودليل التحديثات والصيانة المستمرة للمنصة (Deployment & Logic Rules)

هذا المستند يحدد القواعد الحاكمة والخطوط الحمراء لأي مساعد ذكاء اصطناعي (AI Agent) أو مطور يعمل على منصة د. محمود المهدي، لضمان استقرار المنصة وعدم كسر المنطق التجاري والأمني أثناء التحديثات.

---

## 🚨 1. الخطوط الحمراء (Strict Unmodifiable Core Rules)

### أ) منطق صلاحيات وعزل محتوى الطلاب (Student Content Isolation):
- **قاعدة نظام التعليم الصارمة (Education System Priority)**:
  - طالب البكالوريا (`baccalaureate`) **يُحظر عليه تماماً** رؤية أو الوصول لمواد ومناهج الكليات والجامعات (`university` / `computer-science` / `engineering`) حتى لو ترافقت أرقام الصفوف (مثل "تانية بكالوريا" مع "سنة ثانية جامعي").
  - طالب الجامعة يُحظر عليه رؤية مناهج البكالوريا أو التخصصات والفرق الأخرى التي لم يُسجّل بها.
- **مكان التعديل المحظور إلا بطلب صريح**:
  - دالة `canStudentAccessContent` و `isGradeMatch` في `api-server/src/middleware/student-auth.ts`.
  - دالة `studentCanSeeVideo` في `dr-mahmoud/src/components/YoutubeSection.tsx`.

### ب) منطق الدفع والاشتراكات (Payment & Preview Gating):
- طالب الـ `unpaid`:
  - **يُحظر عليه** تحميل أو فتح أي ملفات/مذكرات تعليمية (`PDF` / `ZIP`) من السيرفر فوراً (`403 PAYMENT_REQUIRED`).
  - الفيديوهات المجانية مقتصرة على **أول فيديوهين فقط** للكورس البكالوريا، و**فيديو واحد فقط** لكورس الجامعة.
  - لا يجوز التعديل على منطق القفل إلا بموافقة صريحة من المستخدم.

---

## 📋 2. خطوات ما قبل أي تحديث أو Deploy (Pre-Deployment Checklist)

قبل تنفيذ أي عملية `git commit` أو `python scripts/deploy.py` أو تعديل في الكود:

1. **الفصل بين التحديثات (Scope Isolation)**:
   - التعديلات البصرية والشكليات (مثل الألوان، الخطوط، مكان العلامة المائية) **يُمنع منعاً باتاً** أن تشمل أي تعديل في ملفات السيرفر أو منطق الـ Authentication والـ Auth Middleware.
2. **فحص الأنواع وتطابق الكود (Type-Checking)**:
   - يجب تشغيل `npx tsc --noEmit` في مجلد الباك إند للتأكد من عدم وجود أخطاء في الـ Types.
   - يجب تشغيل `npm run build` في الفرونت إند للتأكد من خلوه من أي خطل بناء.
3. **الاختبار الذاتي لمنطق العزل (Sanity Check)**:
   - التأكد أن أي دالة تُعدّل الفرونت إند لا تتخطى (`Bypass`) فلترة السيرفر أو تلغي التقييدات.

---

## 🔒 3. بروتوكول الحفظ والنسخ الاحتياطي (Backup Protocol)

- **قبل أي تعديل جوهري**:
  - إنشاء `git tag` بأسماء واضحة (مثل `backup/stable-YYYY-MM-DD`).
  - الاحتفاظ بنسخة مستقرة على `git branch` منفصلة رجوعاً إليها عند حدوث أي طارئ على السيرفر الحي.

---

## 📝 4. سجل الملفات الحساسة (Critical Core Files)

| اسم الملف | الوظيفة | درجة الحساسية |
|-----------|---------|---------------|
| `api-server/src/middleware/student-auth.ts` | منطق صلاحيات وعزل الطلاب | 🔴 حرج جداً |
| `api-server/src/middleware/auth.ts` | حماية جلسات الأدمن وتوقيع التوكينات | 🔴 حرج جداً |
| `dr-mahmoud/src/components/YoutubeSection.tsx` | عرض وتصفية دروس الفيديو | 🔴 حرج جداً |
| `api-server/src/routes/videos.ts` | البث الآمن والبث المباشر للفيديوهات | 🔴 حرج جداً |
| `api-server/src/routes/learning.ts` | تسجيل الطلاب والإيصالات والـ Progress | 🔴 حرج جداً |

---

**ملاحظة:** يتم التقيّد الكامل بهذا المستند في جميع التحديثات القادمة دون أي استثناء.
